# Vendored sherpa-onnx — regenerate with: python tools/setup_sherpa.py
android/engine/src/main/jniLibs/
android/engine/src/main/vendor/

# Downloaded model weights — regenerate with: python tools/fetch_models.py
models/

# Recorded evaluation audio. Keep eval/refs/ (transcripts) in git; audio is
# large, and it is your voice — treat it as personal data, not source.
eval/audio/
eval/benchmark.json
recordings/

# Android / Gradle
*.iml
.gradle/
local.properties
.idea/
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Model transcripts of your recordings (same privacy class as eval/audio/)
eval/fw_*.jsonl
eval/independent_check.json

# Desktop whisper.cpp scratch (binaries + ggml weights)
.tmp_whispercpp/
eval/wcpp_*.jsonl

# Vendored whisper.cpp sources - regenerate with: python tools/setup_whispercpp.py
android/engine/src/main/cpp/whispercpp/

# Downloaded GGML weights - regenerate with: python tools/fetch_ggml_models.py
models_ggml/

# EN->HI MarianMT ONNX weights - regenerate with tools/build_onnx_tokenizer3.py
# + tools/quantize_encoder_with_tokenizer.py (see docs/STATUS.md saga #16-18)
models_mt/
